[package]
name = "axon-providers"
description = "Axon's outward port: the venue-agnostic provider traits (ExecutionClient, MarketData, AccountState, Signer), the normalized order model, and per-adapter capability descriptors. Venue quirks live in the adapters, never here."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
axon-core = { workspace = true }
async-trait = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
